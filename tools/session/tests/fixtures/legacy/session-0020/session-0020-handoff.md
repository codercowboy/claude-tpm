<!-- tpm-session: 0020 · 2026-09-17 · tpm-session-version: 1.0 · files: session-0020-handoff.md, session-0020-punchlist.md, session-0020-log.md -->
> **Session 0020 memory — three files in this folder.  THIS FILE: handoff.**
> • **session-0020-handoff.md** — READ FIRST: where we are, next action, what NOT to redo.
> • **session-0020-punchlist.md** — open/done work items (numbered).
> • **session-0020-log.md** — append-only ledger: Decisions + Log (log reads **bottom-to-top**).

# HANDOFF — session 0020 — 2026-09-17
> ⚠️ READ THIS FULLY before doing anything. Unfinished punchlist items below are required reading.

## Where we are / Next / In flight
**Where we are:** SESSION 020 — the %TPM_HOME% resolution redesign + a deploy/runtime investigation. FULL elaborate record: dev/tpmhome-cache-and-p-findings-20260917/FINDINGS.md (read it first). Summary in three acts: (ACT 1 — shipped at SOURCE level, all committed + `node tools/tests/run-all.js` 8/8) #1096 `tpm home` + #1097 `tpm doc` self-locating primitives; #1099 PostToolUse `expand-tpm-home-content` hook (guard broadened to skip in-bundle read targets); #1098+#1102 workflow round (PASS): project-wide respell ${TPM_HOME}->%TPM_HOME% + ref-lint rule-5 guard + new test + skill methodology read-refs converted to `npx tpm doc`; TITLE_RE reverted to 4-digit + session 019 promoted to 0019; built the dogfood harness tools/tpm-smoke.js. (ACT 2 — the deploy gap, #1104) Claude Code loads a plugin from its CACHE ~/.claude/plugins/cache/..., NOT from source/node_modules; our cache was a Sep-15 snapshot, so NONE of the above was live in any Claude session. `npx tpm install <dir> --force` re-caches from source (plain install skips); cache 0.1.0 is now FRESH. (ACT 3 — the correction, #1105) A fresh cache did NOT make the hook fire: the smoke re-run's probe 2 output was still LITERAL %TPM_HOME%, and probe 1 showed the nested `claude -p` had NO tpm-* skills at all. So `claude -p` (headless) appears not to load plugin skills/hooks. Then the interactive test to settle #1105.B was INCONCLUSIVE: I can launch interactive `claude` in a detached `screen` (it boots + loads the plugin — the folder-trust dialog proves a real interactive session) but CANNOT drive it — `screen -X stuff` keystrokes never reach the TUI (Down/Enter left the trust dialog unchanged over 3 attempts; the TUI reads /dev/tty directly). Also --dangerously-skip-permissions does NOT skip the folder-trust dialog.
**Next action:** ANSWER #1105.B — does the plugin's PostToolUse content hook fire in a real INTERACTIVE session against the fresh cache? Fastest = a HUMAN at the terminal: `cd "/Volumes/My Shared Files/claude/tpm-workspace/tmp/run-cachetest"` -> `claude` -> trust the folder -> ask "Read src-interactive.md and tell me the exact resource path it references" (src-interactive.md is staged with %TPM_HOME%/README.md). RESOLVED path => #1099 is correct for real consumers, only -p/harness is limited; LITERAL token => #1099 needs settings.json delivery (#1105.C). Alt: an `expect` pty-driver (screen-stuff proven not to work). THEN: #1105.D fix the smoke harness (probes 3/4 need acceptEdits/allowedTools; exit-code-0-despite-fails bug; interactive/expect path for plugin probes; clear the trust dialog). ALSO commit the still-unstaged ../claude-tpm pile (#20.1 — USER runs git; it is large now: tpm home/doc, #1099+guard, TITLE_RE/019, the #1098+#1102 round, the read-ref conversions).
**In flight:** Nothing running — all background jobs (smoke harness, 3 screen-drive attempts) completed; reap clean (no screens, no stray claude/node procs). A single fresh interactive claude was NOT left running (each attempt self-quit its screen).

## What remains
- #20.1 · Commit all unstaged ../claude-tpm work: PostToolUse content hook (#1099) + router verb + hooks.json rewire + installer health-check/tests (USER runs git — denied for Claude)  [coglwp, 2026-09-17T15:58:47-07:00]
- #20.4 · #1101: restore session 019 verbose notes/log from the actual chat transcript (over-trimmed by punchlist externalization)  [yx98yy, 2026-09-17T15:58:48-07:00]
- #20.5 · Cleanup (mv to tmp/safe-to-delete — rm denied): orphan claude-context/sessions/.current-session.json + A/B rig tmp/session-tests/ab-20260917  [1u25aj, 2026-09-17T15:58:48-07:00]
- #20.6 · Optional tidy: reword #1099 finished end-action to drop jargon; expand-hook/smoke.sh still exercises the OLD PreToolUse hook (stale, not in run-all)  [1ho8ed, 2026-09-17T15:58:48-07:00]
- #20.7 · #1103: end-to-end live-claude %TPM_HOME% smoke test — the acceptance gate for the whole resolution redesign (rig under ../tmp; run all permission modes)  [jt4coh, 2026-09-17T18:11:00-07:00]
- #20.8 · Verifier non-blocking follow-ups: (A) fix retired expand-tpm-home prose exactness (README/technical/hooks-router:54 say it resolves %TPM_HOME% but its TOKEN is braced-only); (B) add committed test for subagent-prompt lint %TPM_HOME% branch; widen ref-lint/verifier coverage to catch "(path)...Read it" read-refs  [m9kxwr, 2026-09-17T18:11:01-07:00]
- #20.9 · #1104 filed: plugin-cache deploy-correctness — ROOT CAUSE of "content hook never fired": the ~/.claude plugin CACHE was stale (Sep 15); source edits were never re-cached. Fix: `npx tpm install <dir> --force` re-caches from source (cache 0.1.0 now fresh: content hook + migrated skills). Follow-ups A/B/C in #1104 (refresh-verb + doctor stale-cache detection)  [tjzc5l, 2026-09-17T18:46:37-07:00]
- #20.10 · Smoke harness tools/tpm-smoke.js BUILT (#1103 subtask A) + re-running vs tmp/run-cachetest in BACKGROUND (log: tmp/smoke-run-cachetest.log) to confirm probe-2 token-resolution flips FAIL->PASS now the cache is fresh — RESULT PENDING at save time  [5x8313, 2026-09-17T18:46:37-07:00]
- #20.11 · CORRECTION to #20.10: cache refresh did NOT make the content hook fire. Smoke re-run vs FRESH cache 0.1.0 -> probe-2 output still LITERAL %TPM_HOME%; probe-1 friction = "Unknown skill: tpm-session" (nested claude -p had NO plugin skills). So plugin skills/hooks likely dont load under headless claude -p. Filed #1105 to investigate (interactive vs -p; #1099 delivery)  [kzs8kh, 2026-09-17T18:50:49-07:00]
- #20.12 · Smoke harness (#1103) bugs this run exposed: probes 3 (tpm doc) + 4 (tasks) FAILED only because the nested claude was approval-blocked (ran commands never executed) -> pass acceptEdits/allowedTools; harness EXITED 0 despite 3 hard fails (exit-code bug); probes 1/2 cannot validate plugin behavior headlessly if -p doesnt load plugins. See #1105.D  [hebias, 2026-09-17T18:50:49-07:00]
- #20.13 · Interactive (non--p) content-hook test (#1105.B): INCONCLUSIVE. Interactive claude BOOTS + loads the plugin (folder-trust dialog proves a real session) but `screen -X stuff` CANNOT drive the TUI (Down/Enter zero-effect across 3 tries -> TUI reads /dev/tty directly). Trust dialog also blocks (--dangerously-skip-permissions does NOT skip it). Needs a HUMAN at the terminal or an expect pty-driver. Manual check staged: tmp/run-cachetest/src-interactive.md. Full writeup: dev/tpmhome-cache-and-p-findings-20260917/FINDINGS.md  [lwzic1, 2026-09-17T23:30:47-07:00]

## MUST NOT redo
- Do NOT re-conclude 'the cache refresh fixed the hook' — it did NOT. Fresh cache 0.1.0 still failed probe 2 (literal token) and probe 1 showed no plugin skills under `claude -p`.
- Do NOT retry driving the interactive TUI with `screen -X stuff` — proven not to reach the TUI (keystrokes had zero effect over 3 attempts). Use a human at the terminal or `expect`.
- Remember `--dangerously-skip-permissions` does NOT skip the folder-trust dialog — any headless interactive drive must clear that dialog first.
- Do NOT trust the current smoke harness's plugin-behavior verdicts: probes 3/4 failed only because the nested claude was approval-blocked (not real failures); harness exits 0 despite hard fails. Fix per #1105.D first.
- #1096 #1097 #1098 #1099 #1102 are source-correct + committed + npm test 8/8 — do NOT rebuild. Cache 0.1.0 already re-cached; 0.2.0 cruft deleted by user — do not re-run --force unless source changed again.
- Do NOT respell token-matching CODE (hook TOKEN arrays, ref-lint regexes, tests). rm/git/unlink/sudo are DENIED for Claude (personal --settings) — hand git to the user; use `mv` to tmp/safe-to-delete.
