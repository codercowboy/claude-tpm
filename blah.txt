./tools/misc/fix-git-rename/agent-ambiguous-resolution.eval-design.md:- **A4 — variable-prefixed, resolvable stem.** `${TPM_HOME}/tools/x/helper.js` where the *basename*
./tools/misc/fix-git-rename/tpm-fix-git-rename-refs.design.md:special meaning to any project-specific placeholder (e.g. a `${TPM_HOME}`-style variable is just an
./tools/misc/fix-git-rename/tpm-fix-git-rename-refs.design.md:   **domain-agnostic**: it gives no special meaning to any particular variable name (no `${TPM_HOME}`
./tools/misc/fix-git-rename/tpm-fix-git-rename-refs.design.md:  defaults to the current directory. Paths containing variables/placeholders (`${TPM_HOME}`, `${FOO}`,
./tools/tpm.md:**No `%TPM_HOME%` / env needed.** Every router self-locates its scripts from its own `__dirname`, so a
./tools/tpm.md:<verb>` instead of `node %TPM_HOME%/tools/<suite>/<tool>.js` — routing through `tpm` removes the
./tools/tpm.md:`%TPM_HOME%` token from Bash invocations entirely.
./tools/README.md:routers own their verb tables and self-locate their scripts via `__dirname`, so no `%TPM_HOME%`/env is
./tools/README.md:- **`tools/workflow/tpm-workflow-doctor.js`** — fail-loud PREFLIGHT: charters resolve · sign-off writable · compose emits a marker + `%TPM_HOME%` paths; self-locates the bundle, runs in a consumer. Every ✗ prints its fix. Run: `npx tpm workflow doctor` · Doc: header
./tools/README.md:- **`tools/consumer/tpm-consumer-lint-skill-refs.js`** — keep the `tpm-*` skills relocatable: flag any bare bundle ref that must carry `%TPM_HOME%/`. Doc: header
./tools/README.md:- **`tools/consumer/hooks/expand-tpm-home.js`** — PreToolUse hook on `Read|Glob|Grep|NotebookRead`: resolves the `%TPM_HOME%/…` bundle placeholder in read-tool paths to the real bundle (self-located from `__dirname`; read-family only + containment + fail-open). Run: `npx tpm hooks expand-tpm-home` · Doc: header
./tools/task/tests/fixtures/legacy-tasks/clean/bodies/1000-1999/task-1000.md:- **End action:** Relocatable path resolution in workflow tools FIXED: config-resolver charterHome self-locates the bundle (absolute, TPM_BUNDLE_ROOT-overridable); scaffold dropCharter fails loud; compose emits ${TPM_HOME}/ methodology paths. Tests green, E2E from repo + consumer.
./tools/task/tests/fixtures/legacy-tasks/clean/bodies/1000-1999/task-1000.md:**Context:** The ${TPM_HOME} fix covered skill REFERENCES but NOT the workflow tools' own path resolution. Default charter/methodology paths resolve relative to projectRoot (found via the CLAUDE.md marker = the CONSUMER root), but the real resources are vendored under $TPM_HOME/node_modules — so a consumer gets PLACEHOLDER charters + unresolvable methodology paths. Confirmed live in claude-decant. Fix pattern: framework-owned DEFAULT resources self-locate the bundle (via __dirname, like the expand hook), while consumer-config OVERRIDES stay projectRoot-relative. HOLD: shared tools are symlinked into running consumers — never edit mid-round. Audit: tmp/relocatable-tool-audit/candidates.md.
./tools/task/tests/fixtures/legacy-tasks/clean/bodies/1000-1999/task-1000.md:- [x] C. compose-spawn-prompt.js (~:247) — tokenize the 6 emitted claude-context/methodology/ paths to ${TPM_HOME}/ (NEW distinct miss: bare paths baked into every composed spawn prompt, not caught by lint-skill-refs or lint-subagent-prompt). Also extend a lint to flag bare bundle paths in composed prompts.
./tools/task/tpm-task-router.md:Sibling scripts resolve against `__dirname` — no `%TPM_HOME%`/env dependency. Dispatch is by CHILD
./tools/session/tests/fixtures/legacy/session-0020/session-0020-punchlist.md:- [ ] #20.7 · #1103: end-to-end live-claude %TPM_HOME% smoke test — the acceptance gate for the whole resolution redesign (rig under ../tmp; run all permission modes)  [jt4coh, 2026-09-17T18:11:00-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-punchlist.md:- [ ] #20.8 · Verifier non-blocking follow-ups: (A) fix retired expand-tpm-home prose exactness (README/technical/hooks-router:54 say it resolves %TPM_HOME% but its TOKEN is braced-only); (B) add committed test for subagent-prompt lint %TPM_HOME% branch; widen ref-lint/verifier coverage to catch "(path)...Read it" read-refs  [m9kxwr, 2026-09-17T18:11:01-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-punchlist.md:- [ ] #20.11 · CORRECTION to #20.10: cache refresh did NOT make the content hook fire. Smoke re-run vs FRESH cache 0.1.0 -> probe-2 output still LITERAL %TPM_HOME%; probe-1 friction = "Unknown skill: tpm-session" (nested claude -p had NO plugin skills). So plugin skills/hooks likely dont load under headless claude -p. Filed #1105 to investigate (interactive vs -p; #1099 delivery)  [kzs8kh, 2026-09-17T18:50:49-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-punchlist.md:- [x] #20.2 · #1098 is now LOAD-BEARING: migrate skill Read %TPM_HOME%/ paths to npx tpm doc — the removed PreToolUse path-hook no longer resolves token READ-PATHS for consumers  [f8egdr, 2026-09-17T15:58:47-07:00] (closed 2026-09-17T18:10:59-07:00)
./tools/session/tests/fixtures/legacy/session-0020/session-0020-punchlist.md:- [x] #20.3 · #1102: project-wide respell %TPM_HOME% literals (avoid bash ${...} emptying); keep token-matching code accepting both spellings  [mh988d, 2026-09-17T15:58:48-07:00] (closed 2026-09-17T18:10:59-07:00)
./tools/session/tests/fixtures/legacy/session-0020/session-0020-handoff.md:**Where we are:** SESSION 020 — the %TPM_HOME% resolution redesign + a deploy/runtime investigation. FULL elaborate record: dev/tpmhome-cache-and-p-findings-20260917/FINDINGS.md (read it first). Summary in three acts: (ACT 1 — shipped at SOURCE level, all committed + `node tools/tests/run-all.js` 8/8) #1096 `tpm home` + #1097 `tpm doc` self-locating primitives; #1099 PostToolUse `expand-tpm-home-content` hook (guard broadened to skip in-bundle read targets); #1098+#1102 workflow round (PASS): project-wide respell ${TPM_HOME}->%TPM_HOME% + ref-lint rule-5 guard + new test + skill methodology read-refs converted to `npx tpm doc`; TITLE_RE reverted to 4-digit + session 019 promoted to 0019; built the dogfood harness tools/tpm-smoke.js. (ACT 2 — the deploy gap, #1104) Claude Code loads a plugin from its CACHE ~/.claude/plugins/cache/..., NOT from source/node_modules; our cache was a Sep-15 snapshot, so NONE of the above was live in any Claude session. `npx tpm install <dir> --force` re-caches from source (plain install skips); cache 0.1.0 is now FRESH. (ACT 3 — the correction, #1105) A fresh cache did NOT make the hook fire: the smoke re-run's probe 2 output was still LITERAL %TPM_HOME%, and probe 1 showed the nested `claude -p` had NO tpm-* skills at all. So `claude -p` (headless) appears not to load plugin skills/hooks. Then the interactive test to settle #1105.B was INCONCLUSIVE: I can launch interactive `claude` in a detached `screen` (it boots + loads the plugin — the folder-trust dialog proves a real interactive session) but CANNOT drive it — `screen -X stuff` keystrokes never reach the TUI (Down/Enter left the trust dialog unchanged over 3 attempts; the TUI reads /dev/tty directly). Also --dangerously-skip-permissions does NOT skip the folder-trust dialog.
./tools/session/tests/fixtures/legacy/session-0020/session-0020-handoff.md:**Next action:** ANSWER #1105.B — does the plugin's PostToolUse content hook fire in a real INTERACTIVE session against the fresh cache? Fastest = a HUMAN at the terminal: `cd "/Volumes/My Shared Files/claude/tpm-workspace/tmp/run-cachetest"` -> `claude` -> trust the folder -> ask "Read src-interactive.md and tell me the exact resource path it references" (src-interactive.md is staged with %TPM_HOME%/README.md). RESOLVED path => #1099 is correct for real consumers, only -p/harness is limited; LITERAL token => #1099 needs settings.json delivery (#1105.C). Alt: an `expect` pty-driver (screen-stuff proven not to work). THEN: #1105.D fix the smoke harness (probes 3/4 need acceptEdits/allowedTools; exit-code-0-despite-fails bug; interactive/expect path for plugin probes; clear the trust dialog). ALSO commit the still-unstaged ../claude-tpm pile (#20.1 — USER runs git; it is large now: tpm home/doc, #1099+guard, TITLE_RE/019, the #1098+#1102 round, the read-ref conversions).
./tools/session/tests/fixtures/legacy/session-0020/session-0020-handoff.md:- #20.7 · #1103: end-to-end live-claude %TPM_HOME% smoke test — the acceptance gate for the whole resolution redesign (rig under ../tmp; run all permission modes)  [jt4coh, 2026-09-17T18:11:00-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-handoff.md:- #20.8 · Verifier non-blocking follow-ups: (A) fix retired expand-tpm-home prose exactness (README/technical/hooks-router:54 say it resolves %TPM_HOME% but its TOKEN is braced-only); (B) add committed test for subagent-prompt lint %TPM_HOME% branch; widen ref-lint/verifier coverage to catch "(path)...Read it" read-refs  [m9kxwr, 2026-09-17T18:11:01-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-handoff.md:- #20.11 · CORRECTION to #20.10: cache refresh did NOT make the content hook fire. Smoke re-run vs FRESH cache 0.1.0 -> probe-2 output still LITERAL %TPM_HOME%; probe-1 friction = "Unknown skill: tpm-session" (nested claude -p had NO plugin skills). So plugin skills/hooks likely dont load under headless claude -p. Filed #1105 to investigate (interactive vs -p; #1099 delivery)  [kzs8kh, 2026-09-17T18:50:49-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- **Decided:** Ran #1098+#1102 as ONE workflow round (builder->verifier, PASS); then gap-fixed skill methodology READ-refs (#1/#4/#6 -> npx tpm doc, #5 -> per-file via-form) — the round respelled read-refs instead of converting them; the verifier coverage grep (Read .*TPM_HOME) missed the "(path)...Read it" phrasing. Only true reader-executed reads converted; tool-path + self-located citations left as %TPM_HOME%  [2026-09-17T18:11:20-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- **Decided:** CORRECTION: the stale cache (#1104) was real but was NOT the cause of "content hook never fires". Smoke re-run vs a FRESH cache STILL failed probe 2 (literal %TPM_HOME% in output-doc), and probe-1 friction shows the nested claude -p had NO tpm-* skills — so plugin skills/hooks likely do not load under headless claude -p at all — I earlier told the user the refresh would fix it; the harness re-run disproves that. Interactive Claude Code DOES load the plugin (this orchestrator has the skills). Open question filed as #1105 (interactive-vs--p plugin loading; #1099 may need settings.json delivery)  [2026-09-17T18:50:48-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- [ADDED] #20.2 [f8egdr] #1098 is now LOAD-BEARING: migrate skill Read %TPM_HOME%/ paths to npx tpm doc — the removed PreToolUse path-hook no longer resolves token READ-PATHS for consumers  [2026-09-17T15:58:47-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- [ADDED] #20.3 [mh988d] #1102: project-wide respell %TPM_HOME% literals (avoid bash ${...} emptying); keep token-matching code accepting both spellings  [2026-09-17T15:58:48-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- [CLOSED] #20.2 [f8egdr] #1098 is now LOAD-BEARING: migrate skill Read %TPM_HOME%/ paths to npx tpm doc — the removed PreToolUse path-hook no longer resolves token READ-PATHS for consumers  [2026-09-17T18:10:59-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- [CLOSED] #20.3 [mh988d] #1102: project-wide respell %TPM_HOME% literals (avoid bash ${...} emptying); keep token-matching code accepting both spellings  [2026-09-17T18:10:59-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- [ADDED] #20.7 [jt4coh] #1103: end-to-end live-claude %TPM_HOME% smoke test — the acceptance gate for the whole resolution redesign (rig under ../tmp; run all permission modes)  [2026-09-17T18:11:00-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- [ADDED] #20.8 [m9kxwr] Verifier non-blocking follow-ups: (A) fix retired expand-tpm-home prose exactness (README/technical/hooks-router:54 say it resolves %TPM_HOME% but its TOKEN is braced-only); (B) add committed test for subagent-prompt lint %TPM_HOME% branch; widen ref-lint/verifier coverage to catch "(path)...Read it" read-refs  [2026-09-17T18:11:01-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- [DONE] Shipped #1096 (tpm home) + #1097 (tpm doc) self-locating primitives (tests 10/10, wired in dispatcher); #1098+#1102 round PASS (project-wide respell + ref-lint guard + new test); TITLE_RE/019 fix; content-hook guard broadened; spawn-prompt lint accepts tpm doc/%TPM_HOME%; skill read-ref gap-fix. Full npm test 8/8 throughout. Filed #1103 (smoke). ALL ../claude-tpm work unstaged (grew since last commit)  [2026-09-17T18:11:21-07:00]
./tools/session/tests/fixtures/legacy/session-0020/session-0020-log.md:- [ADDED] #20.11 [kzs8kh] CORRECTION to #20.10: cache refresh did NOT make the content hook fire. Smoke re-run vs FRESH cache 0.1.0 -> probe-2 output still LITERAL %TPM_HOME%; probe-1 friction = "Unknown skill: tpm-session" (nested claude -p had NO plugin skills). So plugin skills/hooks likely dont load under headless claude -p. Filed #1105 to investigate (interactive vs -p; #1099 delivery)  [2026-09-17T18:50:49-07:00]
./tools/session/tests/fixtures/legacy/session-0021/session-0021-log.md:- [ADDED] #21.4 [gfzpwz] CARRY-FORWARD from session 0020 (untouched this session): #1105.B human-at-terminal content-hook test + commit the ../claude-tpm pile (#20.1) + the rest of the %TPM_HOME% investigation - see session-0020 handoff, not re-litigated here  [2026-09-19T09:40:34-07:00]
./tools/session/tests/fixtures/legacy/session-0021/session-0021-punchlist.md:- [ ] #21.4 · CARRY-FORWARD from session 0020 (untouched this session): #1105.B human-at-terminal content-hook test + commit the ../claude-tpm pile (#20.1) + the rest of the %TPM_HOME% investigation - see session-0020 handoff, not re-litigated here  [gfzpwz, 2026-09-19T09:40:34-07:00]
./tools/session/tests/fixtures/legacy/session-0021/session-0021-handoff.md:- #21.4 · CARRY-FORWARD from session 0020 (untouched this session): #1105.B human-at-terminal content-hook test + commit the ../claude-tpm pile (#20.1) + the rest of the %TPM_HOME% investigation - see session-0020 handoff, not re-litigated here  [gfzpwz, 2026-09-19T09:40:34-07:00]
./.claude/skills/tpm-task/SKILL.md:selector, confirm destructive ops), then forwards the MECHANICS to `%TPM_HOME%/tools/task/tpm-task.js` and relays
./.claude/skills/tpm-task/SKILL.md:All of `%TPM_HOME%/tools/task/` is self-contained (zero shared imports, per `tool-conventions.md` Part I §2).
./.claude/skills/tpm-spawn/SKILL.md:Reference the tools by their promoted path (`%TPM_HOME%/tools/workflow/…`); a skill never bundles its own copy.
./.claude/skills/tpm-spawn/SKILL.md:   manifest, the **live subagent reading-list** (`%TPM_HOME%/claude-context/methodology/subagent/reading-list.md`),
./.claude/skills/tpm-workflow/modes-reconcile.md:Within an epic, a later phase that needs an earlier phase's tool **copies it** into its own `%TPM_HOME%/tools/`
./.claude/skills/tpm-workflow/modes-reconcile.md:   - Apply the promotion criteria + **move-and-breadcrumb** to project-level `%TPM_HOME%/tools/` (leave a
./.claude/skills/tpm-workflow/modes-reconcile.md:   tasks/epics), **project-history**, and **`%TPM_HOME%/tools/README`** (any promoted tools). **NOT `CLAUDE.md`**
./.claude/skills/tpm-workflow/SKILL.md:| **`doctor`** | Fail-loud PREFLIGHT before a round: checks charters resolve, signoff is writable, compose emits a marker + `%TPM_HOME%` paths. No spawn | (inline, below) |
./.claude/skills/tpm-workflow/SKILL.md:- **Tools** (mechanics — reference by their promoted path, all under `%TPM_HOME%/tools/workflow/`):
./.claude/skills/tpm-workflow/SKILL.md:2. Checks: charters resolve · signoff store writable · compose emits a quoted marker + `%TPM_HOME%/…`
./.claude/skills/tpm-workflow/modes-plan.md:> (`%TPM_HOME%/claude-context/methodology/subagent/reading-list.md`). It owns the base chain AND the per-persona
./.claude/skills/tpm-session/modes-open.md:  (`%TPM_HOME%/claude-context/methodology/orchestrator/pre-task-questions.md`) before spawn/plan/
./.claude/skills/tpm-session/SKILL.md:All are self-contained (`%TPM_HOME%/tools/session/`, zero shared imports with `%TPM_HOME%/tools/workflow/` or
./.claude/skills/tpm-session/SKILL.md:`%TPM_HOME%/tools/child-session/`, per `tool-conventions.md` Part I §2). Run every invocation from the project
./.claude/skills/tpm-session/SKILL.md:   `%TPM_HOME%/tools/session/tpm-session-current.js`'s docstring on why this is diagnostic-only, not load-bearing).
./.claude/skills/tpm-session/modes-close.md:   closed (`%TPM_HOME%/tools/session/tpm-session-current.js`'s `sealSession`), so the NEXT bare
./.claude/skills/tpm-session/modes-close.md:  `%TPM_HOME%/claude-context/methodology/orchestrator/session-process.md`. `close` now updates the SAME folder
./docs/technical.md:The design goal that shapes all of this: **nothing in the `tpm …` chain needs `%TPM_HOME%` or any env var.** Every router self-locates its own scripts relative to `__dirname`. You can run `npx tpm …` from anywhere, in any consumer project, and it finds its own pieces. No setup step, no shell rc edits, no "did you export the path?" support questions. The one place a `%TPM_HOME%` placeholder *does* appear is in read-tool paths inside spawn prompts, and there's a hook that resolves it (see [Hooks](#hooks) below).
./docs/technical.md:- **`expand-tpm-home`** - matches `Read|Glob|Grep|NotebookRead` (the read family). It resolves the `%TPM_HOME%/…` bundle placeholder in read-tool paths to the real bundle location, self-located from `__dirname`. It's **read-family only**, does containment checking, and **fails open**: if anything goes sideways, it gets out of the way rather than blocking your read. Script: `tools/consumer/hooks/expand-tpm-home.js`.
./docs/technical.md:That `%TPM_HOME%` placeholder is how spawn prompts can reference bundle docs without knowing where the bundle physically lives: the prompt says `%TPM_HOME%/claude-context/methodology/…`, and the hook rewrites it to the real path at read time. It's the one env-var-shaped thing in the system, and it exists so the *rest* of the system can stay env-var-free.
./tmp/tpm_home_hits.txt:docs/technical.md:The design goal that shapes all of this: **nothing in the `tpm …` chain needs `${TPM_HOME}` or any env var.** Every router self-locates its own scripts relative to `__dirname`. You can run `npx tpm …` from anywhere, in any consumer project, and it finds its own pieces. No setup step, no shell rc edits, no "did you export the path?" support questions. The one place a `${TPM_HOME}` placeholder *does* appear is in read-tool paths inside spawn prompts, and there's a hook that resolves it (see [Hooks](#hooks) below).
./tmp/tpm_home_hits.txt:docs/technical.md:- **`expand-tpm-home`** - matches `Read|Glob|Grep|NotebookRead` (the read family). It resolves the `${TPM_HOME}/…` bundle placeholder in read-tool paths to the real bundle location, self-located from `__dirname`. It's **read-family only**, does containment checking, and **fails open**: if anything goes sideways, it gets out of the way rather than blocking your read. Script: `tools/consumer/hooks/expand-tpm-home.js`.
./tmp/tpm_home_hits.txt:docs/technical.md:That `${TPM_HOME}` placeholder is how spawn prompts can reference bundle docs without knowing where the bundle physically lives: the prompt says `${TPM_HOME}/claude-context/methodology/…`, and the hook rewrites it to the real path at read time. It's the one env-var-shaped thing in the system, and it exists so the *rest* of the system can stay env-var-free.
./tmp/tpm_home_hits.txt:tmp/tpm_home_hits.txt:docs/technical.md:The design goal that shapes all of this: **nothing in the `tpm …` chain needs `${TPM_HOME}` or any env var.** Every router self-locates its own scripts relative to `__dirname`. You can run `npx tpm …` from anywhere, in any consumer project, and it finds its own pieces. No setup step, no shell rc edits, no "did you export the path?" support questions. The one place a `${TPM_HOME}` placeholder *does* appear is in read-tool paths inside spawn prompts, and there's a hook that resolves it (see [Hooks](#hooks) below).
./tmp/tpm_home_hits.txt:tmp/tpm_home_hits.txt:docs/technical.md:- **`expand-tpm-home`** - matches `Read|Glob|Grep|NotebookRead` (the read family). It resolves the `${TPM_HOME}/…` bundle placeholder in read-tool paths to the real bundle location, self-located from `__dirname`. It's **read-family only**, does containment checking, and **fails open**: if anything goes sideways, it gets out of the way rather than blocking your read. Script: `tools/consumer/hooks/expand-tpm-home.js`.
./tmp/tpm_home_hits.txt:tmp/tpm_home_hits.txt:docs/technical.md:That `${TPM_HOME}` placeholder is how spawn prompts can reference bundle docs without knowing where the bundle physically lives: the prompt says `${TPM_HOME}/claude-context/methodology/…`, and the hook rewrites it to the real path at read time. It's the one env-var-shaped thing in the system, and it exists so the *rest* of the system can stay env-var-free.
./tmp/tpm_home_hits.txt:tools/tpm.md:**No `${TPM_HOME}` / env needed.** Every router self-locates its scripts from its own `__dirname`, so a
./tmp/tpm_home_hits.txt:tools/tpm.md:<verb>` instead of `node ${TPM_HOME}/tools/<suite>/<tool>.js` — routing through `tpm` removes the
./tmp/tpm_home_hits.txt:tools/tpm.md:`${TPM_HOME}` token from Bash invocations entirely.
./tmp/tpm_home_hits.txt:tools/README.md:routers own their verb tables and self-locate their scripts via `__dirname`, so no `${TPM_HOME}`/env is
./tmp/tpm_home_hits.txt:tools/README.md:- **`tools/workflow/tpm-workflow-doctor.js`** — fail-loud PREFLIGHT: charters resolve · sign-off writable · compose emits a marker + `${TPM_HOME}` paths; self-locates the bundle, runs in a consumer. Every ✗ prints its fix. Run: `npx tpm workflow doctor` · Doc: header
./tmp/tpm_home_hits.txt:tools/README.md:- **`tools/consumer/tpm-consumer-lint-skill-refs.js`** — keep the `tpm-*` skills relocatable: flag any bare bundle ref that must carry `${TPM_HOME}/`. Doc: header
./tmp/tpm_home_hits.txt:tools/README.md:- **`tools/consumer/hooks/expand-tpm-home.js`** — PreToolUse hook on `Read|Glob|Grep|NotebookRead`: resolves the `${TPM_HOME}/…` bundle placeholder in read-tool paths to the real bundle (self-located from `__dirname`; read-family only + containment + fail-open). Run: `npx tpm hooks expand-tpm-home` · Doc: header
./tmp/tpm_home_hits.txt:tools/misc/fix-git-rename/agent-ambiguous-resolution.eval-design.md:- **A4 — variable-prefixed, resolvable stem.** `${TPM_HOME}/tools/x/helper.js` where the *basename*
./tmp/tpm_home_hits.txt:tools/misc/fix-git-rename/tpm-fix-git-rename-refs.design.md:special meaning to any project-specific placeholder (e.g. a `${TPM_HOME}`-style variable is just an
./tmp/tpm_home_hits.txt:tools/misc/fix-git-rename/tpm-fix-git-rename-refs.design.md:   **domain-agnostic**: it gives no special meaning to any particular variable name (no `${TPM_HOME}`
./tmp/tpm_home_hits.txt:tools/misc/fix-git-rename/tpm-fix-git-rename-refs.design.md:  defaults to the current directory. Paths containing variables/placeholders (`${TPM_HOME}`, `${FOO}`,
./tmp/tpm_home_hits.txt:tools/task/tests/smoke-checklist.md:way `/tpm-spawn` was exercised. This is NOT a unit-test suite (that's `${TPM_HOME}/tools/task/tests/`); it's a set
./tmp/tpm_home_hits.txt:tools/task/tests/smoke-checklist.md:> and read the skill files from `out/skills/tpm-task/`. Post-promotion the paths are `${TPM_HOME}/tools/task/tpm-task.js`
./tmp/safe-to-delete/task-OLD-20260920-212702/tests/smoke-checklist.md:way `/tpm-spawn` was exercised. This is NOT a unit-test suite (that's `%TPM_HOME%/tools/task/tests/`); it's a set
./tmp/safe-to-delete/task-OLD-20260920-212702/tests/smoke-checklist.md:> and read the skill files from `out/skills/tpm-task/`. Post-promotion the paths are `%TPM_HOME%/tools/task/tpm-task.js`
